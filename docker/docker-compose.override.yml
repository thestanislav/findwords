# Optional: copy to docker-compose.override.yml to override env vars from the shell or .env
#   cp docker/docker-compose.override.example.yml docker/docker-compose.override.yml
# Then you can run:
#   APP_DEBUG=0 make restart
# Or put overrides in a .env file in the project root (add .env to .gitignore).
# docker-compose merges override files automatically; environment overrides env_file.
services:
    mezzio-swoole-app:
        environment:
            - APP_DEBUG=${APP_DEBUG:-1}
            - APP_ENV=${APP_ENV:-development}
